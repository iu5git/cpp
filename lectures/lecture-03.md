###### *Лекции по курсу "Основы программирования МГТУ им. Баумана кафедра ИУ5*

# Лекция 3. Циклы

**Дисциплина:** Основы программирования на C++
**Продолжительность:** 2 × 45 минут

Главная идея лекции:

> **Цикл позволяет выполнить один и тот же фрагмент программы несколько раз, пока выполняется некоторое условие.**
Сформировать понимание **цикла как алгоритмической конструкции**: условие → тело → изменение состояния → повторная проверка.

----------

# Цели лекции

После занятия студент должен:

- понимать, зачем нужны циклы;
- уметь определять условие продолжения цикла;
- знать `while`, `do while` и `for`;
- понимать различие между ними;
- уметь использовать `break` и `continue`;
- понимать вложенные циклы;
- уметь находить бесконечные циклы;
- понимать типичные ошибки при работе с циклами;
- познакомиться со статическими локальными переменными;
- понимать базовую связь между областью видимости и временем жизни переменной.

----------

# План

### Первая часть — 45 минут

[1. Зачем нужны циклы](#1-зачем-нужны-циклы)

[2. Цикл как алгоритм](#2-цикл-как-алгоритм)

[3. `while`](#3-while)

[4. Бесконечные циклы](#4-бесконечные-циклы)

[5. `do while`](#5-do-while)

### Вторая часть — 45 минут

[6. Цикл `for`](#6-цикл-for)

[7. Циклы и управление их выполнением](#7-циклы-и-управление-их-выполнением)

[8. Вложенные циклы](#8-вложенные-циклы)

[9. Итоги](#9-итоги)

----------

# 1. Зачем нужны циклы

Допустим, необходимо вывести числа от 1 до 5.

Можно написать:

```cpp
std::cout << 1 << '\n';
std::cout << 2 << '\n';
std::cout << 3 << '\n';
std::cout << 4 << '\n';
std::cout << 5 << '\n';

```

Работает.

Но что если нужно вывести числа от 1 до 1 000 000?

Очевидно, такой подход не подходит.

Нам нужно описать **правило**, а не перечислить миллион команд.

Например:

> Начать с 1 и каждый раз увеличивать число на 1, пока оно не станет больше 1 000 000.

Вот это и есть идея цикла.

----------

# 2. Цикл как алгоритм

Любой цикл можно представить в виде одной из трех схем:

![](/flowcharts-organization-cycle.png)

Например:

```cpp
int i{1};

while (i <= 5) {
    std::cout << i << '\n';
    ++i;
}
```

### До начала цикла

```cpp
int i{1};
```

`i` равно `1`.

### Проверка

```cpp
i <= 5
```

Истина.

### Выполнение тела

```cpp
std::cout << i << '\n';
```

### Изменение состояния

```cpp
++i;
```

Теперь `i == 2`.

Снова проверяем условие.

И так далее.

----------

# 3. `while`

Общий вид:

```cpp
while (условие) {
    // тело цикла
}
```

Смысл:

> Пока условие истинно, выполнять тело цикла.

----------

## Важное свойство `while`

Условие проверяется **до** выполнения тела.

Поэтому тело может не выполниться ни одного раза.

```cpp
int x{10};

while (x < 5){
    std::cout << x << '\n';
}
```

Ничего не произойдет.

----------

## Состояние цикла

> Чтобы цикл когда-нибудь закончился, его состояние должно изменяться так, чтобы условие в какой-то момент стало ложным.

Например:

```cpp
int i{1};

while (i <= 10) {
    std::cout << i << '\n';
    ++i;
}
```

Здесь состояние — `i`.

Мы изменяем его:

```cpp
    ++i;
```

----------

## Распространенные ошибки

```cpp
int i{10};

while (i > 0) {
    std::cout << i << '\n';
    ++i;
}
```

`i` увеличивается, хотя для завершения цикла его нужно уменьшать.

----------

А здесь:

```cpp
int i{1};

while (i <= 10) {
    std::cout << i << '\n';
}
```

состояние не меняется.

Получаем бесконечный цикл.

----------

# 4. Бесконечные циклы

Бесконечный цикл — это цикл, условие которого никогда не становится ложным.

Например:

```cpp
while (true) {
    // ...
}
```

Это **намеренно** бесконечный цикл.

----------

# 5. `do while`

Иногда необходимо, чтобы тело цикла выполнилось **как минимум один раз**.

Тогда используется:

```cpp
do {
    // тело
} while (условие);
```

Например:

```cpp
int x{0};

do {
    std::cout << "Введите положительное число: ";
    std::cin >> x;
} while (x > 0);
```

Здесь проверка выполняется **после** тела.

----------

## Главное отличие

**`while`**

```text
проверка
   ↓
  тело
   ↓
проверка
```

**`do while`**

```text
тело
    ↓
проверка
    ↓
тело
```

Поэтому:

```cpp
int x{10};

while (x < 5) {
    std::cout << "while\n";
}
```

ничего не выведет,

a:

```cpp
int x{10};

do {
    std::cout << "do while\n";
} while (x < 5);
```

выведет:

```text
do while
```

ровно один раз.

----------

## Когда использовать `do while`

Например, при организации меню

```cpp
...
int choice{};

do {
    // показать меню
    // получить choice

} while (choice != 0);
```

Пользователь должен увидеть меню хотя бы один раз.

> **Однако**
Профессиональные разработчики и стандарты кодирования (например, Google C++ Style Guide) рекомендуют избегать `do-while` по нескольким веским причинам:

***1. Логическая инверсия кода*** В цикле `for` или `while` условие находится сверху. Вы сразу видите, при каком условии выполнится код.В `do-while` тело цикла выполняется вслепую, а условие проверки спрятано в самом низу. Если тело цикла длинное (больше 10–15 строк), человеку приходится прокручивать код вниз, чтобы понять, почему этот цикл вообще крутится. Это снижает читаемость.

***2. Опасность «первой бесплатной итерации*»** Главная фишка `do-while` — он всегда выполняется минимум один раз, даже если условие изначально ложно. В современном программировании это частый источник багов: Если перед циклом данные оказались некорректными (например, файл не открылся), `do-while` всё равно пойдет выполнять код и вызовет критическую ошибку (Crash). Обычный `while` сначала проверит и просто пропустит цикл.

***3. Проблемы с областью видимости (Scope)*** Переменные, объявленные внутри тела `do-while`, не видны в условии проверки.

```cpp
do {
    int x{3};
} while (x != 0); // ОШИБКА: компилятор не знает, что такое x!
```

Используйте код с осторожностью. Чтобы код заработал, переменную приходится выносить наверх, за пределы цикла.

----------

# Первая половина: ключевой вывод

`while`

> может выполнить тело 0 или больше раз.

`do while`

> выполняет тело 1 или больше раз.

----------

# Перерыв

----------

# 6. Цикл `for`

`for` особенно удобен, когда структура цикла естественно выражается через начальное состояние, условие продолжения и изменение состояния — например, при переборе диапазона или использовании счётчика.

Например:

> вывести числа от 1 до 10.

```cpp
for (int i{1}; i <= 10; ++i) {
    std::cout << i << '\n';
}
```

Общий вид:

```cpp
for (инициализация; условие; изменение) {
    // тело
}
```

Три части:

```cpp
for (int i{1}; i <= 10; ++i)
```

### *1. Инициализация*

```cpp
int i{1};
```

Выполняется один раз.

### *2. Условие*

```cpp
i <= 10 ;
```

Проверяется перед каждой итерацией.

### *3. Изменение*

```cpp
++i
```

Выполняется после тела.

----------

# 7. Циклы и управление их выполнением

***Итерация*** — одно выполнение тела цикла.

Например:

```cpp
for (int i{1}; i <= 3; ++i) {
    std::cout << "i = " << i << '\n';
}
```

имеет три итерации:

```text
i = 1
i = 2
i = 3
```

После `i == 4` условие:

```cpp
i <= 3
```

становится ложным.

Цикл заканчивается.

----------

***`break`***

`break` немедленно завершает цикл.

```cpp
for (int i{0}; i < 10; ++i) {
    if (i == 5) {
        break;
    }
    std::cout << i << '\n';
}
```

Вывод:

```text
0
1
2
3
4
```

Когда `i == 5`, выполняется `break`.

Программа выходит из цикла.

----------

>Важно

`break` завершает **только ближайший цикл**.

Если есть вложенные циклы:

```cpp
for (...) {
    for (...) {
        break;
    }
}
```

будет завершен внутренний цикл.

----------

***`continue`***

`continue` не завершает цикл.

Он говорит:

> Пропустить оставшуюся часть текущей итерации и перейти к следующей.

Например:

```cpp
for (int i{1}; i <= 10; ++i) {
    if (i % 2 == 0) {
        continue;
    }

    std::cout << i << '\n';
}
```

Вывод:

```text
1
3
5
7
9
```

----------

**`break` и `continue`**

```text
break
    ↓
выйти из цикла
```

```text
continue
    ↓
перейти к следующей итерации
```

----------

***`for` и `while`***

Эти циклы могут выполнять одну и ту же задачу.

Например:

```cpp
int i{1};

while (i <= 5) {
    std::cout << i << '\n';
    ++i;
}
```

и:

```cpp
for (int i{1}; i <= 5; ++i) {
    std::cout << i << '\n';
}

```

Результат одинаковый.

Разница в том, что `for` компактно объединяет:

- начальное состояние;
- условие;
- изменение состояния.

----------

***Какой цикл использовать?***

**`for`** - Когда есть понятный счетчик или диапазон, когда заранее известно количество итераций.

```cpp
for (int i{0}; i < 10; ++i)
```

**`while`** - Когда повторение зависит от некоторого условия и количество итераций заранее неизвестно.

```cpp
while (value != 0){
    ...
}
```

**`do while`** - Когда тело должно выполниться хотя бы один раз.

----------

***Область видимости переменной цикла***

```cpp
for (int i{0}; i < 10; ++i) {
    std::cout << i << '\n';
}

```

После цикла переменная `i` больше не существует.

Она имеет область видимости внутри `for`.

# 8. Вложенные циклы

Цикл может находиться внутри другого цикла.

Например, таблица:

```cpp
for (int i{1}; i <= 3; ++i) {
    for (int j{1}; j <= 3; ++j) {
        std::cout << i * j << ' ';
    }
    std::cout << '\n';
}
```

Внешний цикл отвечает за строки.

Внутренний — за элементы строки.

**Как это работает**

При:

```text
i = 1
```

внутренний цикл полностью выполняется:

```text
j = 1
j = 2
j = 3
```

Затем:

```text
i = 2
```

и внутренний цикл снова проходит:

```text
j = 1
j = 2
j = 3
```

И так далее.

----------
<!--
# ???? после лекции с функциями


# 14. Статические локальные переменные.      ???? после лекции с функциями

Обычная локальная переменная:

```cpp
void foo() {
    int count{0};
    ++count;

    std::cout << count << '\n';
}
```

Каждый вызов функции создает новую переменную `count`.

Если вызвать:

```cpp
    foo();
    foo();
    foo();
```

получим:

```text
1
1
1
```

----------

Теперь:

```cpp
void foo() {
    static int count{0};
    ++count;

    std::cout << count << '\n';
}
```

Результат:

```text
1
2
3
```

Потому что `static` меняет **время жизни** локальной переменной.

----------

## Очень важно различать

**Область видимости** и **время жизни** — это не одно и то же.

В примере:

```cpp
void foo() {
    static int count{0};
}

```

`count`:

- видна только внутри `foo`;
- существует всё время работы программы.

То есть:

```text
scope
    ↓
где переменную можно использовать

lifetime
    ↓
как долго переменная существует

```

Это пока только первое знакомство с понятиями. Подробно к ним вернемся при изучении функций и организации программы.

----------

# 15. Где полезен `static`

Например, нужно посчитать количество вызовов функции:

```cpp
void printCallCount() {
    static int count{0};

    ++count;

    std::cout << "Вызов № " << count << '\n';
}

```

Это демонстрационный пример.

В реальном современном коде глобальное состояние и скрытое состояние функций нужно использовать осторожно.

Главное сейчас — понять **время жизни переменной**.

----------

# 16. Типичные ошибки студентов

### Ошибка 1. Точка с запятой после `while`

```cpp
while (i < 10);
{
    std::cout << i;
}
```

Это уже другая программа.

Пустая команда является телом `while`.

----------

### Ошибка 2. Забыли изменить счетчик

```cpp
int i{0};

while (i < 10) {
    std::cout << i;
}

```

Бесконечный цикл.

----------

### Ошибка 3. Изменили не в ту сторону

```cpp
int i{10};

while (i > 0) {
    ++i;
}

```

Условие никогда не станет ложным.

----------

### Ошибка 4. Ошибка границы

```cpp
for (int i{0}; i <= 10; ++i)

```

Здесь будет **11 итераций**:

```text
0 ... 10
```

Если нужно 10 итераций:

```cpp
for (int i{0}; i < 10; ++i)
```

Получаем:

```text
0 ... 9
```

----------

# 17. Отладка цикла

Здесь хорошо показать GDB / debugger из первой лекции.

Например:

```cpp
for (int i{0}; i < 5; ++i) {
    std::cout << i << '\n';
}

```

Поставить breakpoint.

Показывать:

```text
i = 0
i = 1
i = 2
...

```

И задать студентам вопрос:

> В какой момент проверяется `i < 5`?

> Когда выполняется `++i`?

> Сколько раз выполнится тело?

Это гораздо полезнее, чем просто показать синтаксис `for`.

----------

# 18. Практика в конце занятия

Я бы дал 4 небольших задачи по возрастающей сложности.

### Задача 1

Вывести числа от 1 до 100.

----------

### Задача 2

Вывести все четные числа от 1 до 100.

----------

### Задача 3

Найти сумму чисел от 1 до `N`.

Например:

```text
N = 5

1 + 2 + 3 + 4 + 5 = 15

```

----------

### Задача 4

Пользователь вводит число `N`.

Вывести все делители этого числа.

Например:

```text
N = 12

1 2 3 4 6 12

```

Здесь студенты уже должны самостоятельно объединить:

```text
for
if
%

```

----------

# Контрольные вопросы в конце

Я бы закончил лекцию не пересказом материала, а вопросами.

### 1.

Чем отличается `while` от `do while`?

### 2.

Сколько раз выполнится:

```cpp
int i{10};

while (i < 5) {
    ++i;
}

```

### 3.

Что произойдет здесь?

```cpp
int i{0};

while (i < 10) {
    std::cout << i;
}

```

### 4.

Чем отличаются `break` и `continue`?

### 5.

Сколько итераций?

```cpp
for (int i{0}; i < 10; ++i)

```

### 6.

Почему переменная

```cpp
for (int i{0}; ...)

```

недоступна после цикла?

### 7.

Чем отличается обычная локальная переменная от локальной `static`?

----------
-->

# 9. Итоги

> **Цикл — это не просто синтаксис `for` или `while`. Это способ описать повторение действий на основе некоторого изменяющегося состояния.**

Tри конструкции:

```text
while
    └── повторять, пока условие истинно

do while
    └── выполнить хотя бы один раз, затем повторять

for
    └── удобно описывать цикл с инициализацией,
        условием и изменением счетчика

```

a `break` и `continue` — это уже **управление ходом выполнения цикла**.

```text
scope     <= где имя доступно
lifetime  <= как долго объект существует
```

Пока без глубокого погружения. В следующих лекциях эти два понятия станут ключом к пониманию **ссылок, указателей, `static`, динамической памяти и RAII**.
